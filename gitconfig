[user]
    name = Diana Flach
[push]
    default = simple

[alias]
    co = checkout
    cob = checkout -b
    cm = commit -a
    undo = reset HEAD~1 --mixed
    amend = commit -a --amend
    st = status
    minit = !git init $1 && git symbolic-ref HEAD refs/heads/main

[core]
    editor = vim
